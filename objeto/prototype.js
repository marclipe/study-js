//Herdar novas funcionalidades de outro objeto 
const frutas = ['laranja', 'manga', 'morango']
frutas.filter(function(fruta) {
    return fruta === 'manga'
})

//Todo objeto que você cria no JS ele linka para o prototype dele para herdar novas propriedades
const array = new Array
console.log(array)

